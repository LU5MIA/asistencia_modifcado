<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="BottomNavViewOverlay.NoRipple" parent="">
        <item name="android:colorControlHighlight">@android:color/transparent</item> <!-- Ripple -->
        <item name="itemRippleColor">@android:color/transparent</item> <!-- Solo Material3 -->
        <item name="itemBackground">@android:color/transparent</item> <!-- Fondo -->
    </style>
</resources>